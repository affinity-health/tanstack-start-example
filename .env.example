# Authenticate once with `bun alchemy login --configure`.
ALCHEMY_PROFILE=default
CLOUDFLARE_ACCOUNT_ID=3704af15f1156ed64a2150672c74248c

# Affinity production-hosted Test credentials. Keep these server-side.
AFFINITY_API_KEY=sk_test_replace_me
AFFINITY_WEBHOOK_SECRET=whsec_test_replace_me

# One demo provider's verified mapping. This is a durable resource ID, not a secret.
# Real platforms store and authorize one mapping ID per provider instead.
AFFINITY_PROVIDER_MAPPING_ID=pmap_replace_me

# Prefer a synthetic patient in a state where the demo provider is licensed.
AFFINITY_DEMO_PATIENT_STATE=CA
